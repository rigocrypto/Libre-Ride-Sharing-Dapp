# Screenshot Assets

Drop product screenshots here so the root `README.md` can render them. The README
references these exact filenames — capture them from the live GitHub Pages demo and
commit the PNGs to replace the TODO placeholders.

| Filename | Capture from | Shows |
| --- | --- | --- |
| `landing.png` | `/` | Landing / hero |
| `founding-access.png` | `/founding-access` | Founding Driver registration form |
| `rider-dashboard.png` | `/rider` | Orlando rider dashboard |
| `rider-profile.png` | `/rider/profile` | Rider profile / account |
| `driver-dashboard.png` | `/driver` | Driver operations dashboard |
| `admin-dashboard.png` | `/admin` | Admin escrow lifecycle monitoring |

Guidelines:

- Use real screenshots from the deployed app — do not fabricate UI.
- Prefer a consistent viewport (e.g. 1440px wide desktop) for a clean grid.
- Optimize PNGs before committing (keep each under ~500 KB where possible).
- Never include real lead data, emails, or secrets in a screenshot.
